# Hardening Phase Sign-Off - 2026-03-04

## Document Control

- Status: Approved for frontend testing
- Date: 2026-03-04
- Scope: `.planning/phases/phase-hardening/H1` through `H7`
- Dataset profile: production-like rehydrate from live sources plus local GIAS CSV fallback

## Rehydrate Execution

Environment used:

- `CIVITAS_BRONZE_ROOT=data/bronze-signoff-20260304-r2`
- `CIVITAS_GIAS_SOURCE_CSV=data/bronze/gias/2026-03-03/edubasealldata.csv`
- `CIVITAS_DFE_CHARACTERISTICS_BACKFILL_ENABLED=true`

Rehydrate result (latest successful runs):

- `gias`: succeeded (`downloaded=52271, staged=50529, promoted=50529, rejected=1742`)
- `dfe_characteristics`: succeeded (`downloaded=32817, staged=16408, promoted=16408, rejected=0`)
- `dfe_characteristics backfill --lookback-years 5`: skipped no change (expected on immediate rerun)
- `ons_imd`: succeeded (`downloaded=67511, staged=33755, promoted=33755, rejected=0`)
- `ofsted_latest`: succeeded (`downloaded=21966, staged=21966, promoted=21966, rejected=0`)
- `ofsted_timeline`: succeeded (`downloaded=49461, staged=49461, promoted=49461, rejected=0`)
- `police_crime_context`: succeeded (`downloaded=17937898, staged=17668708, promoted=17856880, rejected=269190`)

Police source archive used by runtime metadata:

- `https://data.police.uk/data/archive/2026-01.zip`

Operational steady state after cleanup:

- `pipeline_runs(status='running') = 0`
- `pipeline_source_locks = 0`

## H7 Quality Gate Evidence

## Gate 0 - Run Policy And Contracts

Passed:

- `uv run --project apps/backend pytest apps/backend/tests/unit/test_pipeline_runner.py -q`
- `uv run --project apps/backend pytest apps/backend/tests/unit/test_pipeline_cli.py -q`
- `uv run --project apps/backend python tools/scripts/verify_source_contracts_runtime.py`

## Gate 1 - Historical Backfill And Trend Depth

Passed:

- `uv run --project apps/backend pytest apps/backend/tests/integration/test_dfe_characteristics_pipeline.py -q`
- `uv run --project apps/backend pytest apps/backend/tests/integration/test_school_trends_repository.py -q`
- `uv run --project apps/backend civitas pipeline backfill --source dfe_characteristics --lookback-years 5`

Note:

- The original H7 command used `--dry-run`; CLI does not support that flag. Gate command was executed with the supported backfill invocation.

## Gate 2 - Completeness Across API/UI

Passed:

- `uv run --project apps/backend python tools/scripts/export_openapi.py`
- `cd apps/web && npm run generate:types`
- `uv run --project apps/backend pytest apps/backend/tests/integration/test_school_profile_api.py apps/backend/tests/integration/test_school_trends_api.py -q`
- `cd apps/web && npm run test -- school-profile`
- `cd apps/web && npm run lint`
- `cd apps/web && npm run typecheck`

## Gate 3 - Freshness And Coverage Observability

Passed:

- `uv run --project apps/backend pytest apps/backend/tests/unit/test_data_quality_use_cases.py -q`
- `uv run --project apps/backend pytest apps/backend/tests/integration/test_data_quality_repository.py -q`
- `uv run --project apps/backend python tools/scripts/check_data_quality_slo.py --strict`

## Gate 4 - Resilience And Performance

Passed:

- `uv run --project apps/backend pytest apps/backend/tests/unit/test_pipeline_retry_policy.py -q`
- `uv run --project apps/backend pytest apps/backend/tests/integration/test_pipeline_resume.py -q`
- `uv run --project apps/backend python tools/scripts/run_pipeline_recovery_drill.py --strict`
- `uv run --project apps/backend python tools/scripts/benchmark_pipeline_throughput.py --strict`

Key outputs:

- Recovery drill: `first_status=failed resumed_status=succeeded`
- Throughput benchmark: `status=succeeded rows_per_second=8697467.29`

## Gate 5 - Golden Path

Passed:

- `make lint`
- `make test`

## Data Layer Spot Checks

## Bronze

Latest bronze partition contains metadata/manifests for all sources under:

- `data/bronze-signoff-20260304-r2/<source>/2026-03-04/*`

Checked metadata files include:

- `edubasealldata.metadata.json`
- `school_characteristics.metadata.json` and `school_characteristics.backfill.manifest.json`
- `file_7.metadata.json`
- `latest_inspections.metadata.json`
- `assets.manifest.json` (8 timeline assets)
- `archive.metadata.json` (police archive URL and timestamp)

## Silver

- `staging` schema is empty post-promote (expected cleanup behavior).
- Rejection groups are consistent with contract checks:
  - `gias / stage / missing_coordinates = 1742`
  - `police_crime_context / stage / missing_longitude = 269190`

## Gold

Current row counts:

- `schools = 50529`
- `school_demographics_yearly = 16408`
- `school_ofsted_latest = 21966`
- `ofsted_inspections = 49461`
- `area_deprivation = 33755`
- `area_crime_context = 17854864`

Temporal coverage checks:

- Ofsted timeline: `2015-09-14` to `2026-01-15`
- Crime context: `2023-02` to `2026-01`
- Demographics academic years present: `2024/25`

API spot check (`URN 100000`):

- `GET /api/v1/schools/100000` -> `200`
- `GET /api/v1/schools/100000/trends` -> `200`
- Profile completeness includes explicit statuses (`partial`/`available`) with reason codes and update timestamps.

## Data Quality Snapshot (Strict)

Latest strict snapshot:

- demographics coverage: `16408/50529` (`0.3247`)
- ofsted latest coverage: `21966/50529` (`0.4347`)
- ofsted timeline coverage: `24715/50529` (`0.4891`)
- deprivation coverage: `3/50529` (`0.0001`)
- crime coverage: `50316/50529` (`0.9958`)
- run health: all sources `consecutive_failed_runs=0`

Alert status:

- Warning only: `sparse_trend_risk` for demographics (source currently provides one-year trend depth).

## Residual Risks And Follow-Ups

1. `area_deprivation` profile coverage remains low because join depends on cached postcode-to-LSOA mapping coverage; this is transparent in API completeness as `not_joined_yet` when missing.
2. Demographic trends remain sparse (`2024/25` only) and are surfaced as a warning, not silent data loss.
3. Integration tests that run against shared local DB can mutate production-like rows (fixed URNs); use a dedicated isolated test database for future sign-off runs.

## Final Decision

All H7 gates are green with a successful end-to-end rehydrate and verified cross-layer spot checks. The hardening phase is signed off and the dataset is ready for frontend testing.
