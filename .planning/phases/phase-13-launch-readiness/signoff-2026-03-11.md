# Phase 13 Sign-Off - 2026-03-11

## Scope Closed

- Static launch pages: About, Data Sources, Contact, Privacy, Terms, Accessibility
- Route wiring, footer links, and sign-in legal disclosures
- Page metadata, canonical behavior, crawler assets, and school-profile JSON-LD
- Phase documentation/status alignment

## Findings Closed

1. Legal pages were structurally incomplete against `L4-legal-and-compliance.md`.
   - Added explicit legal-basis, retention, cookie-lifetime, governing-law, enforcement, and review-date sections.
2. The Data Sources page did not list the leaver-destinations datasets already used by the school profile.
   - Added KS4 and 16 to 18 destinations source rows with publisher links and cadence.
3. Canonical/crawler-asset and JSON-LD behavior lacked direct automated coverage.
   - Added focused tests for site config asset generation and `SchoolJsonLd`.
4. The phase folder still reported the work as planned.
   - Updated `README.md` and `L1` through `L5` statuses to implemented.

## Validation Evidence

- `make lint` - passed
- `make test` - passed
- `cd apps/web && npm run build` - passed

## Notes

- Validation was run with `VITE_PUBLIC_URL` unset for the standard build, which correctly produced a non-indexable `robots.txt` and an empty static-route sitemap.
- A separate build check with `VITE_PUBLIC_URL=https://schools.example.test` confirmed canonical-origin asset generation for `robots.txt`, `sitemap.xml`, and fallback Open Graph metadata.
- The web build still reports the existing large MapLibre chunk warning; it did not block Phase 13 sign-off.
